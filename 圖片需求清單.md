# 圖片需求清單

> 更新於 2026-08-27

## 怎麼補圖

把圖檔放進 `public/images/`，檔名等於下表的「檔名代號」加上副檔名即可，
**不用改任何程式**。副檔名可以是 `.jpg` / `.jpeg` / `.png` / `.webp`。

```
public/images/card-results.jpg
```

存好之後重新整理瀏覽器（開發模式會自動重載）。還沒補的位置會顯示一個
灰色規格框，寫著「待補圖」，不會破版。

### ⚠️ 檔名一定要全部小寫

`Card-when.jpg` 在你的 Mac 上會正常，但**放到網路主機（Linux）上就會失效**，
圖片會默默變回「待補圖」空框。Mac 的檔案系統不分大小寫，Linux 分。
所以檔名請一律用小寫，跟下表一模一樣。

---

## 進度

**已完成 4 張，還缺 7 張**（原本 17 個位置，其中 6 個位置與其他位置共用同一張圖）。

### ✅ 已補

| 檔名代號 | 圖片內容 | 用在哪裡 |
|---|---|---|
| `card-about` | 骨盆全景 + 正常髖關節 vs 髖關節發育不良 對照 | 首頁卡片①、`/about-ddh` 的「球與碗」插圖 |
| `card-screening` | 嬰兒接受髖關節超音波檢查 | 首頁卡片②、`/screening` 的「檢查當天」插圖 |
| `card-when` | 各月齡檢查方式時間軸 | 首頁卡片③、`/when-to-screen` 的時間軸插圖 |
| `card-results` | 示意報告單：分型 Type I、α 角 64°、判讀正常 | 首頁卡片④ |

### ⬜ 待補

★ = 優先。首頁最多人看，最後一張首頁卡片先補效益最大。

| 優先 | 檔名代號 | 圖片內容 | 比例 | 建議尺寸 | 用在哪裡 |
|---|---|---|---|---|---|
| ★ | `card-daily` | 寶寶雙腿自然彎曲外展成 M 字型 | 16:9 | 1200×675 | 首頁卡片⑤ |
| ★ | `graf-angles` | Graf 量法：基準線、骨性髖臼頂（Alpha 角）、軟骨頂（Beta 角） | 4:3 | 1000×750 | `/results` |
| ★ | `three-habits` | 三格並排：包開開、抱開開、背開開，每格標出 M 字腿 | 21:9 | 1400×600 | 首頁、`/daily-care` |
| | `graf-types` | Graf 各分型的髖臼形狀對照（I、IIa、IIb、IIc、D、III、IV） | 16:9 | 1200×675 | `/results` |
| | `hip-baby-vs-adult` | 嬰兒髖關節（大部分是軟骨）與成人髖關節（已骨化）對照 | 16:9 | 1200×675 | `/about-ddh` |
| | `us-vs-xray` | 同一個髖關節：X 光看不到軟骨（留白），超音波看得到 | 16:9 | 1200×675 | `/screening` |
| | `risk-factors` | 危險因子一覽：臀位產、家族史、女嬰、第一胎、羊水過少、冬天出生、多胞胎 | 4:3 | 1000×750 | `/when-to-screen` |
| | `swaddle-do-dont` | 包巾對照：錯誤（雙腿拉直併攏綁緊）vs 正確（包上不包下） | 16:9 | 1200×675 | `/daily-care` |
| | `carry-do-dont` | 抱姿對照：錯誤（橫抱、雙腿併攏）vs 正確（直立式、雙腿跨開） | 16:9 | 1200×675 | `/daily-care` |
| | `carrier-do-dont` | 揹巾對照：錯誤（窄底、雙腿懸垂成 I 字）vs 正確（寬基底、膝蓋略高於臀部） | 16:9 | 1200×675 | `/daily-care` |

---

## 比例對不上怎麼辦

不用自己去裁圖。`lib/images.ts` 裡每個位置可以設定兩種顯示方式：

| 設定 | 意思 | 什麼時候用 |
|---|---|---|
| `fit: "cover"`（預設） | 填滿整個框，超出的邊裁掉 | 照片、畫面本來就滿版的插圖 |
| `fit: "contain"` + `mat` | 整張圖完整顯示，比例不合時左右或上下留邊，`mat` 是留邊的顏色 | **圖上有文字標示的圖解**，否則標示會被裁掉 |

補圖之後如果覺得被裁掉了重要的東西，跟我說是哪一張，我改一行就好。

---

## 已知的一個判斷點

`card-when`（時間軸）圖上有一個紅色的「警示馬上執行超音波」方塊。
這張圖同時出現在**首頁**與 `/when-to-screen`，而這次改版刻意讓
「寶寶大多正常」這條動線上**完全不出現紅色**（紅色全站只保留給「看到什麼 →
立刻回診」）。醫學上這個標示是正確的，但它會是首頁上唯一的紅色元素。
若你希望首頁換一張沒有紅框的圖，把新圖存成 `card-when.jpg` 覆蓋即可；
`/when-to-screen` 要另外用原圖的話跟我說，我把兩個位置拆開。

---

## `card-results` 是用程式畫的，不是插畫

這張是我用 `scripts/make_card_results.py` 畫出來的，跟前三張的手繪醫學插畫
風格不一樣——它是「網站介面」的長相，不是插畫。原因是這台環境沒有生圖工具，
我無法產生同樣風格的插畫。

要重畫的話有兩條路：

1. **改文字或配色**：跟我說要改什麼，我改腳本重跑 `python3 scripts/make_card_results.py`，
   會直接覆蓋 `public/images/card-results.jpg`。
2. **換成手繪插畫**：用你畫前三張的工具，提示詞可以用下面這段，
   畫好之後存成 `card-results.jpg` 覆蓋即可。

> Medical illustration in the same style as a pediatric orthopedic atlas:
> a printed hip ultrasound report sheet lying on a table, held at one corner
> by a parent's hand. On the sheet, two fields are circled by hand in pencil —
> one reading "Type I", one reading "α 64°". Soft ivory-cream background
> (#FDFDEF), muted skin and paper tones, thin black leader lines with
> arrowheads pointing to the two circled fields, bilingual labels in
> Traditional Chinese with English in parentheses below. Clean, calm,
> reassuring. No hospital logo, no patient name. 16:9 landscape, 1600×900.

**注意**：如果請 AI 生圖，圖上的中文字常常會變成亂碼或錯字。
生完務必逐字看過，尤其是 Type、α、度數這幾個關鍵字。
