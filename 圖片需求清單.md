# 圖片需求清單

> 進度表由程式自動產生，最後更新：2026-08-27

## 怎麼補圖

把圖檔放進 `public/images/`，檔名等於下表的「檔名代號」加上副檔名。
**網站會自動抓到，不用改任何程式、也不用重開伺服器**，重新整理瀏覽器就看得到。

```
public/images/card-daily.jpg
```

### 但是「自動」有一個前提：檔名必須一模一樣

程式是拿代號去資料夾裡「找同名檔案」，找不到就當作那張圖不存在，
畫面上維持灰色空框，**而且不會有任何錯誤訊息**。所以只要檔名差一個字：

| 你存成 | 應該是 | 結果 |
|---|---|---|
| `graft-angle.jpg` | `graf-angles.jpg` | 不會上線 |
| `card-result.jpg` | `card-results.jpg` | 不會上線 |
| `Card-when.jpg` | `card-when.jpg` | 在你的 Mac 上正常，**上線到網路主機會失效** |
| `card-about 2.jpg` | `card-about.jpg` | 不會上線 |
| `替換圖片/card-about.jpg` | 要放在最上層 | 不會上線（不會找子資料夾） |

補完之後跑這一行，它會列出「放進去但不會被用到」的檔案並猜你想放哪個位置：

```
python3 scripts/update_image_checklist.py
```

（同時也會更新下面那張進度表。）

### 三個容易踩到的地方

1. **檔名全部小寫。** Mac 的檔案系統不分大小寫，Linux 分。
   大寫檔名在你電腦上看起來正常，部署上去就默默失效。
2. **Finder 問要不要取代時選「取代」。** 讓它存成「檔名 2.jpg」就不會上線。
3. **檔案要放在 `public/images/` 最上層**，放進子資料夾不會被找到。

---

## 進度

<!-- AUTO -->
**17 個圖片位置，已補上 8 個**（實際只用了 5 張圖，其中 3 個位置與別的位置共用同一張），**還缺 9 個**。

★ ＝ 優先補。首頁曝光最高，或是該頁最關鍵的一張。

| 狀態 | 檔名代號 | 圖片內容 | 比例 | 尺寸 | 用在哪裡 |
|---|---|---|---|---|---|
| ✅ 已補 | `card-about` | 骨盆全景，兩側放大對照：正常髖關節 vs 髖關節發育不良 | 16/9 | 2752×1536・2007 KB | 首頁卡片① |
| ✅ 已補 | `card-screening` | 嬰兒側躺、探頭貼在髖部，接受髖關節超音波檢查 | 16/9 | 1024×572・93 KB | 首頁卡片② |
| ✅ 已補 | `card-when` | 各月齡檢查方式時間軸：理學檢查 → 超音波 → X 光 | 16/9 | 2752×1536・2026 KB | 首頁卡片③ |
| ✅ 已補 | `card-results` | 示意報告單：分型 Type I、α 角 64°、判讀正常 | 16/9 | 2400×1350・168 KB | 首頁卡片④ |
| ★ 待補 | `card-daily` | 寶寶雙腿自然彎曲外展成 M 字型 | 16/9 | 建議 1200×675 | 首頁卡片⑤ |
| ✅ 與 `card-about` 共用 | `hip-ball-socket` | 正常髖臼（碗夠深）與發育不良（碗太淺）並排對照 | 16/9 | 2752×1536・2007 KB | `/about-ddh` |
| ⬜ 待補 | `hip-baby-vs-adult` | 嬰兒髖關節（大部分是軟骨）與成人髖關節（已骨化）對照 | 16/9 | 建議 1200×675 | `/about-ddh` |
| ⬜ 待補 | `us-vs-xray` | 同一個髖關節：X 光看不到軟骨（留白），超音波看得到 | 16/9 | 建議 1200×675 | `/screening` |
| ✅ 與 `card-screening` 共用 | `us-exam-day` | 檢查當天的實際情形：寶寶側躺、探頭貼在髖部 | 16/9 | 1024×572・93 KB | `/screening` |
| ✅ 與 `card-when` 共用 | `timeline-4-6w` | 時間軸：各月齡分別用理學檢查、超音波、X 光 | 16/9 | 2752×1536・2026 KB | `/when-to-screen` |
| ⬜ 待補 | `risk-factors` | 危險因子一覽：臀位產、家族史、女嬰、第一胎、羊水過少、冬天出生、多胞胎 | 4/3 | 建議 1000×750 | `/when-to-screen` |
| ✅ 已補 | `graf-angles` | 超音波影像上的 Graf 量法：基準線、α 角、β 角，右側附骨骼對照 | 16/9 | 2754×1536・2480 KB | `/results` |
| ⬜ 待補 | `graf-types` | Graf 各分型的髖臼形狀對照（I、IIa、IIb、IIc、D、III、IV） | 16/9 | 建議 1200×675 | `/results` |
| ★ 待補 | `three-habits` | 三格並排：包開開、抱開開、背開開，每格標出 M 字腿 | 21/9 | 建議 1400×600 | 首頁、`/daily-care` |
| ⬜ 待補 | `swaddle-do-dont` | 包巾對照：左為錯誤（雙腿拉直併攏綁緊），右為正確（包上不包下，下半身留空間） | 16/9 | 建議 1200×675 | `/daily-care` |
| ⬜ 待補 | `carry-do-dont` | 抱姿對照：左為錯誤（橫抱、雙腿併攏），右為正確（直立式、雙腿跨開） | 16/9 | 建議 1200×675 | `/daily-care` |
| ⬜ 待補 | `carrier-do-dont` | 揹巾對照：左為錯誤（窄底、雙腿懸垂成 I 字），右為正確（寬基底、膝蓋略高於臀部） | 16/9 | 建議 1200×675 | `/daily-care` |
<!-- /AUTO -->

上面這張表是跑 `python3 scripts/update_image_checklist.py` 產生的。
補完圖之後執行一次，狀態、實際尺寸、用在哪幾頁都會自動更新，不用手動改。

### 沒被用到的檔案

<!-- AUTO-ORPHAN -->
以下檔案放在 `public/images/` 裡，但**網站不會用到**：

| 檔案 | 為什麼沒被用到 | 是不是想放這個位置？ |
|---|---|---|
| `替換圖片/card-result.jpg` | 放在子資料夾裡，而且檔名沒有對應的位置 | `card-results` |
| `替換圖片/card-about.jpg` | 放在子資料夾裡，網站只看 public/images/ 最上層 | `card-about` |
| `替換圖片/card-result1.jpg` | 放在子資料夾裡，而且檔名沒有對應的位置 | `card-results` |

改成右邊那一欄的檔名（副檔名不用改）就會自動上線。
<!-- /AUTO-ORPHAN -->

---

## 比例對不上怎麼辦

不用自己去裁圖。`lib/images.ts` 裡每個位置可以設定兩種顯示方式：

| 設定 | 意思 | 什麼時候用 |
|---|---|---|
| `fit: "cover"`（預設） | 填滿整個框，超出的邊裁掉 | 照片、畫面本來就滿版的插圖 |
| `fit: "contain"` + `mat` | 整張圖完整顯示，比例不合時左右或上下留邊，`mat` 是留邊的顏色 | **圖上有文字標示的圖解**，否則標示會被裁掉 |

補圖之後如果覺得被裁掉了重要的東西，跟我說是哪一張，我改一行就好。

另外，表格裡標「與 ⋯⋯ 共用」的位置，代表它跟另一個位置用同一個檔案
（例如首頁卡片和內頁插圖其實是同一張圖），不需要另外準備。
如果想讓兩處用不同的圖，跟我說一聲我把它們拆開。

---

## 已知的一個判斷點

`card-when`（時間軸）圖上有一個紅色的「警示馬上執行超音波」方塊。
這張圖同時出現在**首頁**與 `/when-to-screen`，而這次改版刻意讓
「寶寶大多正常」這條動線上**完全不出現紅色**（紅色全站只保留給「看到什麼 →
立刻回診」）。醫學上這個標示是正確的，但它會是首頁上唯一的紅色元素。
若你希望首頁換一張沒有紅框的圖，把新圖存成 `card-when.jpg` 覆蓋即可；
`/when-to-screen` 要另外用原圖的話跟我說，我把兩個位置拆開。

---

## `card-results` 是用程式畫的，不是插畫

這張是我用 `scripts/make_card_results.py` 畫出來的，跟其他幾張的手繪醫學插畫
風格不一樣——它是「網站介面」的長相，不是插畫。原因是這台環境沒有生圖工具，
我無法產生同樣風格的插畫。

要重畫的話有兩條路：

1. **改文字或配色**：跟我說要改什麼，我改腳本重跑 `python3 scripts/make_card_results.py`，
   會直接覆蓋 `public/images/card-results.jpg`。
2. **換成手繪插畫**：用你畫其他幾張的工具，提示詞可以用下面這段，
   畫好之後存成 `card-results.jpg` 覆蓋即可。

> Medical illustration in the same style as a pediatric orthopedic atlas:
> a printed hip ultrasound report sheet lying on a table, held at one corner
> by a parent's hand. On the sheet, two fields are circled by hand in pencil —
> one reading "Type I", one reading "α 64°". Soft ivory-cream background
> (#FDFDEF), muted skin and paper tones, thin black leader lines with
> arrowheads pointing to the two circled fields, bilingual labels in
> Traditional Chinese with English in parentheses below. Clean, calm,
> reassuring. No hospital logo, no patient name. 16:9 landscape, 1600×900.

**注意**：如果請 AI 生圖，圖上的中文字常常會變成亂碼或錯字。
生完務必逐字看過，尤其是 Type、α、度數這幾個關鍵字。
