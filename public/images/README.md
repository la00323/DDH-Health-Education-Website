把圖檔放進這個資料夾就會自動顯示，不用改任何程式。

檔名必須完全等於「圖片需求清單.md」裡的代號，副檔名可以是
.jpg / .jpeg / .png / .webp。例如：card-about.jpg

⚠️ 檔名一定要「全部小寫」。Card-about.jpg 在 Mac 上看起來正常，
但放到網路主機（Linux）上會失效，圖片會默默變回「待補圖」空框。

放好之後重新整理頁面即可（開發模式會自動重載）。
